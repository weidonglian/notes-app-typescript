overwrite: true
schema: "./src/graphql/*.graphql"
documents: null
generates:
  ./src/graphql/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-document-nodes"
  ./graphql.schema.json:
    plugins:
      - "introspection"
